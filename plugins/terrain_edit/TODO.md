# TerrainEditPlugin — 优化待办

> 持续维护，每完成一项标记 [x] 并附时间戳。

## UI/UX

- [x] **工具面板不够直观** — [2026-05-08] 已加 QPainter 程序化图标（6个），按钮文字+图标布局，激活态视觉反馈
- [x] **笔刷光标不跟随地形** — [2026-05-08] 采样48个圆周点地形高度的3D笔刷预览
- [x] **网格线过多** — [2026-05-08] 基于相机距离的 LOD 网格（step 1/2/4/8），小地图自动降采样
- [x] **纹理颜色实时反馈** — [2026-05-08] 工具栏增加纹理/高度着色切换按钮
- [x] **窗口状态持久化** — [2026-05-08] QSettings 保存/恢复窗口位置、工具、笔刷参数、纹理选择
- [x] **快捷键支持** — [2026-05-08] 1-5 工具切换，[/] 笔刷大小，Shift+[/] 笔刷强度

## 国际化/本地化

- [x] **插件级别汉化** — [2026-05-08] 创建 `translations/terrain_edit_zh_CN.ts`（22条翻译），CMake + lrelease 自动编译 .qm，插件 init 时自动加载系统语言对应翻译，destroy 时清理

## 编辑功能

- [x] **撤销/重做 (Undo/Redo)** — [2026-05-08] Terrain 快照栈（最多60步），鼠标按下时自动保存，Ctrl+Z/Y 操作，重做栈在新建作时清空
- [x] **笔刷形状** — [2026-05-08] 圆形/方形切换按钮 + B 快捷键
- [ ] **选区/笔刷形状** — 仅支持圆形/方形笔刷。建议：支持自由套索/按玩家颜色选区，编辑只影响选区内顶点
- [x] **地形边界限制** — [2026-05-08] applyBrush 循环自带 qBound 边界裁剪，不会越界
- [x] **高度值限制** — [2026-05-08] 所有编辑操作结果经 clampHeight 限制在 ±8192（w3e short 安全范围）

## 渲染

- [ ] **无俯览小地图** — 缺少全局俯览图来定位。建议：右下角叠加一个 MiniMap，显示地形高度/纹理缩略图和当前视口位置
- [ ] **无 LOD (Level of Detail)** — 大地图全部顶点一次性上传。建议：根据相机距离动态简化远处网格，或使用分块渲染
- [x] **着色器单一** — [2026-05-08] 增加三种渲染模式（线框/无光照/光照），Lambertian 方向光，法线基于邻域高度差分

## 相机

- [ ] **相机无惯性/平滑** — 拖动停止后视角立即停止。建议：加入阻尼惯性，拖动停止后缓慢减速
- [x] **无聚焦功能** — [2026-05-08] 按 F 聚焦到当前悬停 tile，cam_distance_ 缩小一半
- [ ] **俯仰角度限制可调** — 固定 ±89°。建议：允许用户配置角度限制，或增加顶视图模式

## 数据

- [x] **编辑统计信息** — [2026-05-08] 加载时状态栏显示 min/max/range height，鼠标悬停时显示 tile 坐标/高度/纹理 ID
- [ ] **编辑器数据面板** — 没有显示当前选中 tile 的详细数据。建议：点击时显示 TilePoint 全部字段（height, water, texture, flags 等）

## 构建/部署

- [ ] **依赖缺失时缺少提示** — 若运行时缺少 Qt OpenGL DLL，静默失败。建议：加载时检查依赖并给出友好提示
- [ ] **插件版本与主程序版本校验** — 无版本兼容检查。建议：插件暴露最低主程序版本号，不匹配时拒绝加载

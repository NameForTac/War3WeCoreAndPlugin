#pragma once

// Shared types for the terrain editor plugin

enum class EditTool { Raise, Lower, Smooth, Flatten, Paint };
enum class BrushShape { Circle, Square };
enum class RenderMode { Wireframe, Unlit, Lit };
